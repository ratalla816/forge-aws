
<div >
  {{#each this}}
  <section class="mb-3 ml-3 b">
    <div  class="mt-3 ml-5">
      <b>{{user.username}} on {{format_date created_at}}</b>
    </div>
    <div class="p-5">
      {{comment_body}}
    </div>
  </section>
  {{/each}}
</div>